import { API_PREFIX } from "./paths";

/** IAM 베이스 경로 — 가입·승인·로그인 등 */
export const IAM_BASE = API_PREFIX.IAM;

// TODO: OpenAPI / FRONTEND_API.md 확정 후
// export async function postSignup(...) { return apiFetch(`${IAM_BASE}/...`, { method: "POST", json: {...} }); }

